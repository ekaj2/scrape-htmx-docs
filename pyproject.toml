[tool.poetry]
name = "doc-scrape"
version = "0.1.0"
description = ""
authors = ["Jake Duth"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.11"
scrapy = "^2.11.2"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
